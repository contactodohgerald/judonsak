<?php

use Illuminate\Database\Seeder;
use App\Models\Charge_type;

class Charge_typeSeeder extends Seeder
{
    // public function run()
    // {
    //     if (Charge_type::count() == 0) {
    //         $data = json_decode(File::get('database/list/charge_types.json'));
    //         foreach ($data as $obj) {
    //             Charge_type::updateOrcreate([
    //                 'name'     => $obj->name,
    //                 'description'  => $obj->description
    //             ]);
    //         }

    //     }
    // }

    // Seeder class as been abandoned and archived for delete...remember to delete this file before production
}
